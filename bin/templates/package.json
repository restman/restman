{
  "name": "YOURAPPNAME",
  "version": "0.1.0",
  "description": "Your rest api description.",
  "author": "",
  "license": "MIT",
  "privite": true,
  "main": "index.coffee",
  "scripts": {
    "start": "coffee index.coffee",
    "watch": "NODE_ENV=development nodemon --watch app --watch lib index.coffee",
    "test": "npm run test-spec && npm run test-api",
    "test-api": "NODE_ENV=test mocha test/api/*.coffee",
    "test-spec": "NODE_ENV=test mocha test/spec/*.coffee"
  },
  "dependencies": {
    "restman": "latest",
    "lodash": "3.10.0"
  },
  "devDependencies": {
    "coffee-script": "1.9.3",
    "nodemon": "1.4.0",
    "mocha": "2.2.5",
    "should": "7.0.1",
    "supertest": "1.0.1"
  }
}
